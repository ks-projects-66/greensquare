# Astro build (this is now a real Astro project; package.json and
# package-lock.json ARE tracked so the build is reproducible)
node_modules/
dist/
.astro/
.vercel/
/design-language/src/env.d.ts

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Local QA artefacts
/_qa/
/screenshots/
/.impeccable/
/.design-sync/
/.playwright-mcp/
# Screenshots and the report written by qa/run-launch-qa.mjs. CI uploads these as a
# build artefact; running the same gate locally must not leave binaries in a public repo.
/qa/launch/

# Internal / commercial docs — keep OUT of this public repo.
# Canonical home is the private repo ks-projects-66/greensquare-ops (+ OneDrive copies).
/CLAUDE.md
/docs/strategy/
/docs/superpowers/

# Local AI tooling config (personal, not for the public repo)
/.claude/
/.codex/

# claude.ai/design sync — generated output and machine state.
# The durable inputs under .design-sync/ (config.json, NOTES.md, conventions.md,
# entry.tsx, foundations/, previews/, docs/, brand-layer.css, build-css.mjs)
# ARE tracked so a re-sync is reproducible.
/.ds-sync/
/ds-bundle/
/.design-sync/.cache/
/.design-sync/learnings/
/.design-sync/node_modules
.env*
